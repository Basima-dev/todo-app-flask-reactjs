services:
  frontendo: 
    image: frontendo
    ports:
      - 5173:5173
  backendo:
    image: backendo
    ports:
      - 5000:5000
    environment:
      - FLASK_RUN_HOST=0.0.0.0
      - FLASK_RUN_PORT=5000
      - JWT_SECRET_KEY=${JWT_SECRET_KEY} 
  
